# Copy this file to .env and adjust values. .env is git-ignored on purpose --
# never commit real secrets.

APP_NAME=TaskFlow API
DEBUG=True

# --- Postgres (used starting Phase 2 -- ignored for now) ---
POSTGRES_USER=taskflow
POSTGRES_PASSWORD=taskflow
POSTGRES_DB=taskflow
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
